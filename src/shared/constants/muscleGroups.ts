/**
 * Grupo muscular por defecto cuando un ejercicio no se puede clasificar
 * (imports, creación rápida, y todos los fallbacks de analítica).
 *
 * Debe existir como opción válida en la lista de grupos (`MUSCLE_GROUPS` en
 * ExerciseSelector) y coincidir con el default del trigger de servidor
 * `autoclassify_muscle_group`. Si cambia aquí, cámbialo también allí.
 */
export const DEFAULT_MUSCLE_GROUP = 'Otro';
